{{> logo-header}}

<main class="resultaten">

    {{!-- {{#each vacatures}} --}}
    <div>
        <h2 class="functie">UX Designer</h2>
        <h3 class="bedrijfsnaam">Philips</h3>
        <h4 class="plaatsnaam">Haarlem</h4>
        <h4 class="salaris">3000.- per maand</h4>

        <p class="description">Philips is een groot bedrijf in het hart van Haarlem en ze zijn momenteel op zoek naar
            een nieuwe UX Designer.</p>

        {{!-- de vormgeving van deze button is het zelfde als alle andere divs omdat deze button
        in een div staat, ik ga de styling NOG niet aanpassen van deze button, want straks gaat het fout ofzo x ashley:)
        --}}
        <div class="opslaanButton">
            <form action="/opgeslagenvacatures" method="POST">
                <button type="submit" name="userid" value={{_id}}>opslaan</button>
            </form>
        </div>
    </div>

    <div></div>

    <div>
    </div>

    <div></div>
    {{!-- {{/each}} --}}
</main>

{{> navigatie-footer}}